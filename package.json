{
  "name": "firestore-erd-plus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k -n FRONTEND,BACKEND -c blue,green \"pnpm frontend\" \"pnpm backend\"",
    "frontend": "pnpm --filter frontend dev",
    "backend": "dotnet watch run --project backend",
    "old_backend": "pnpm --filter old_backend dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.23.0",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}